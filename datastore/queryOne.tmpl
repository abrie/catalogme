func (datastore *Datastore) Get{{.ObjType}}(id string) (*{{.ObjType}}, error) {
	var (
		obj {{.ObjType}}
	)

	err := datastore.DB.QueryRow("select {{.SelectParams}} from {{.TableName}} where ROWID=?", id).Scan({{.ScanParams}})
	if err != nil {
		log.Fatal(err)
	}

	return &obj, nil
}
